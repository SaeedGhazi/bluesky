# MANUAL — پیکربندی شبکهٔ چند-اپراتوره BlueSky (تک‌سرور در برابر چندسرور مستقل)

> **فرض این سند:** `RCP` = ایستگاه کنترل رادار (نمایشگر `flat(BlueSky).html` که
> کنترلر با آن کار می‌کند)، `BD` = یک نمونهٔ اپلیکیشن BlipDriver (کنسول خلبان مجازی).
> اگر منظور شما از RCP چیز دیگری است، اصلاح کنید تا سند به‌روزرسانی شود.

> **منبع مکانیزم شبکهٔ BlueSky:** بررسی مستقیم [`BlueSky.py`](https://github.com/TUDelft-CNS-ATM/bluesky/blob/master/BlueSky.py)،
> [`run-client-on-windows.bat`](https://github.com/TUDelft-CNS-ATM/bluesky/blob/master/run-client-on-windows.bat)
> و `settings.cfg` واقعی این پروژه.

---

## سناریوی مورد نظر شما

```
گروه ۱:  RCP1 + BD1 + BD2
گروه ۲:  RCP2 + BD3 + BD4
```
همه روی یک شبکهٔ محلی (LAN) واحد.

دو حالت:
- **الف)** یک BlueSky، داده برای همه پخش می‌شود، همهٔ BD ها به همان یک BS فرمان می‌فرستند.
- **ب)** دو BlueSky مستقل — گروه ۱ با BS-1، گروه ۲ با BS-2.

---

## بخش ۰ — مفاهیم پایهٔ شبکهٔ BlueSky (لازم برای فهم هر دو حالت)

BlueSky بین **هستهٔ شبیه‌سازی (sim/server)** و **کلاینت‌ها** (GUI یا ابزارهای بیرونی
مثل Bridge ما) با **ZMQ روی TCP** ارتباط برقرار می‌کند:

| مفهوم | توضیح |
|---|---|
| Server / sim node | پروسه‌ای که شبیه‌سازی واقعی در آن اجرا می‌شود؛ روی `recv_port`/`send_port` (در پروژهٔ ما: ۱۱۰۰۰/۱۱۰۰۱) گوش می‌دهد |
| Client | GUI بومی BlueSky، یا هر ابزار بیرونی (مثل `bluesky-bridge.py` ما) که به server وصل می‌شود |
| `python BlueSky.py` | اجرای server + client (GUI) در یک پروسه — حالت پیش‌فرض تک‌کاربره |
| `python BlueSky.py --headless` | فقط **server** (بدون GUI) — برای اجرای شبیه‌سازی روی یک ماشین مرکزی |
| `python BlueSky.py --client <IP>` | فقط **client** — به یک server از راه دور با IP مشخص وصل می‌شود |
| Discovery | قابلیت پیداکردن خودکار server در شبکه (broadcast)؛ در `settings.cfg` این پروژه **غیرفعال** است (`enable_discovery` پیش‌فرض False) — یعنی همیشه باید IP/پورت صریح داده شود |

**نکتهٔ کلیدی برای هر دو حالت:** آنچه در پروژهٔ شما «شبکه» را عملاً می‌سازد، **آدرس IP و
پورتی است که `bluesky-bridge.py` برای اتصال ZMQ به core استفاده می‌کند**، نه صرفاً
خود BlueSky GUI. پس تنظیمات کلیدی در دو جا اعمال می‌شوند:
1. `settings.cfg` هر نمونهٔ BlueSky (پورت‌ها)
2. متغیرهای host/port در بالای `bluesky-bridge.py` (`ZMQ_CORE_HOST`, `ZMQ_EXTRAS_HOST`, `WS_PORT`)

---

## حالت الف — یک BlueSky مشترک برای همه

### ۱. توپولوژی

```
                     ┌─────────────────────────┐
                     │   ماشین مرکزی BS          │
                     │   IP: 192.168.1.10        │
                     │                          │
                     │  BlueSky.py --headless   │
                     │  atc_extras.py (11005)   │
                     │  ZMQ 11000 / 11001       │
                     └──────────┬────────────────┘
                                │
                     ┌──────────┴────────────────┐
                     │  bluesky-bridge.py         │
                     │  (می‌تواند روی همان ماشین    │
                     │   یا ماشین دیگر اجرا شود)   │
                     │  WebSocket :8080           │
                     └──────────┬────────────────┘
           ┌────────────────────┼────────────────────┐
           ▼                    ▼                     ▼
     RCP1 (مرورگر)        BD1 (پایتون)           BD2 (پایتون)
           ▼                                          ▼
     RCP2 (مرورگر)        BD3                     BD4
```

همهٔ اجزا (RCP1, RCP2, BD1..BD4) از **همان یک** WebSocket (`ws://192.168.1.10:8080`)
داده می‌گیرند و فرمان BD ها (طبق تصمیم قبلی پروژه) از طریق لایهٔ میانی به همین یک BS
می‌رسد.

### ۲. تنظیمات اپلیکیشن

**روی ماشین مرکزی (`192.168.1.10`):**

`settings.cfg` (بدون تغییر نسبت به وضعیت فعلی پروژه):
```ini
recv_port = 11000
send_port = 11001
enabled_plugins = ['area', 'datafeed', 'atc_extras']
```

اجرا:
```bash
# روی ماشین مرکزی — بدون GUI (headless) چون فقط محاسبه لازم است
cd bluesky-master/
python BlueSky.py --headless

# در کنسول (یا از طریق اسکریپت راه‌انداز):
PLUGIN LOAD ATC_EXTRAS
```

**بریج (روی همان ماشین یا ماشین دیگر — مثلاً یک سرور نمایش):**

اگر Bridge روی ماشین دیگری اجرا می‌شود، در `bluesky-bridge.py` آدرس‌ها را به IP
ماشین مرکزی تغییر دهید:
```python
ZMQ_CORE_HOST   = "tcp://192.168.1.10:11000"
ZMQ_EXTRAS_HOST = "tcp://192.168.1.10:11005"
WS_HOST         = "0.0.0.0"     # روی همهٔ اینترفیس‌ها گوش بده
WS_PORT         = 8080
```

اجرا:
```bash
python bluesky-bridge.py
```

**روی هر ماشین کلاینت (RCP یا BD):**

- `flat(BlueSky).html` → در کد آن، `WS_URL` را به IP بریج تنظیم کنید:
  ```js
  const WS_URL = "ws://192.168.1.10:8080";
  ```
- اپلیکیشن BD → همین آدرس WebSocket را برای دریافت ترافیک استفاده می‌کند؛ برای
  ارسال فرمان (لایهٔ میانی) هم به همین IP وصل می‌شود (پورت لایهٔ میانی — طبق
  `BD_UI_SPEC.md`، هنوز نهایی نشده، ولی روی همین ماشین مرکزی خواهد بود).

### ۳. تنظیمات شبکه

| مورد | مقدار |
|---|---|
| IP ماشین مرکزی | ثابت باشد (Static IP یا DHCP Reservation) — مثلاً `192.168.1.10` |
| پورت‌های باز لازم (Firewall inbound روی ماشین مرکزی) | `11000/tcp`, `11001/tcp`, `11005/tcp`, `8080/tcp` |
| Subnet | همهٔ اجزا باید در همان subnet باشند یا مسیر routing بین‌شان برقرار باشد (بدون NAT مخرب) |
| تست اتصال | از هر کلاینت: `nc -zv 192.168.1.10 8080` (یا `Test-NetConnection` در ویندوز) |

**نکته:** چون `enable_discovery = False` است، هیچ broadcast خودکاری رخ نمی‌دهد —
همه‌چیز صراحتاً با IP کار می‌کند؛ این برای این حالت هم ساده‌تر و هم قابل‌اطمینان‌تر است.

---

## حالت ب — دو BlueSky مستقل روی یک شبکه

### ۱. توپولوژی

```
گروه ۱                                    گروه ۲
┌─────────────────────────┐              ┌─────────────────────────┐
│ BS-1  (192.168.1.10)     │              │ BS-2  (192.168.1.20)     │
│ recv=11000 send=11001    │              │ recv=12000 send=12001    │
│ atc_extras → 11005       │              │ atc_extras → 12005       │
└──────────┬────────────────┘              └──────────┬────────────────┘
           │                                           │
┌──────────┴────────────────┐              ┌──────────┴────────────────┐
│ bridge-1 → WS :8080        │              │ bridge-2 → WS :8081        │
└──────────┬────────────────┘              └──────────┬────────────────┘
     ┌──────┴──────┐                             ┌──────┴──────┐
     ▼             ▼                             ▼             ▼
   RCP1      BD1, BD2                          RCP2      BD3, BD4
```

**اصل طلایی این حالت:** هر جفت (BS + atc_extras + bridge) باید مجموعه‌پورت **کاملاً
مجزا** داشته باشد تا با یکدیگر تداخل نکنند — حتی اگر هر دو BS روی یک ماشین اجرا شوند.

### ۲. جدول پورت‌بندی پیشنهادی

| جزء | گروه ۱ (BS-1) | گروه ۲ (BS-2) |
|---|---|---|
| `recv_port` (BlueSky) | 11000 | 12000 |
| `send_port` (BlueSky) | 11001 | 12001 |
| ZMQ PUB پلاگین `atc_extras` | 11005 | 12005 |
| WebSocket بریج | 8080 | 8081 |
| IP پیشنهادی (اگر ماشین جدا) | 192.168.1.10 | 192.168.1.20 |

### ۳. تنظیمات اپلیکیشن — گام به گام

#### الف) دو نسخهٔ مجزای BlueSky

چون BlueSky مسیر پلاگین/کش را **نسبت به working directory** حل می‌کند (نکتهٔ
شناخته‌شدهٔ این پروژه)، **هر BS باید در پوشهٔ کاری مستقل خودش اجرا شود** — حتی روی
یک ماشین:

```bash
# کپی یا clone دوم از فورک شما، در مسیر جدا
git clone git@github.com:SaeedGhazi/bluesky.git bluesky-group2
```

#### ب) `settings.cfg` گروه ۱ (`bluesky-group1/settings.cfg`)

```ini
recv_port = 11000
send_port = 11001
enabled_plugins = ['area', 'datafeed', 'atc_extras']
start_location = 'OIII'
```

#### ج) `settings.cfg` گروه ۲ (`bluesky-group2/settings.cfg`)

```ini
recv_port = 12000
send_port = 12001
enabled_plugins = ['area', 'datafeed', 'atc_extras']
start_location = 'OIII'
```

#### د) اصلاح `atc_extras.py` برای پورت غیرثابت (مهم!)

پلاگین فعلی مستقیم `socket.bind("tcp://*:11005")` دارد — این باید per-instance
قابل تنظیم شود، وگرنه دومین اجرا با خطای «Address already in use» مواجه می‌شود
(حتی اگر روی ماشین‌های مختلف باشند، اگر ماشین یکسان باشد قطعاً؛ اگر ماشین‌های جدا
باشند مشکلی نیست، ولی برای انعطاف بهتر است پارامتری شود). دو گزینه:

**گزینهٔ ۱ (ساده — ماشین‌های جدا):** اگر هر BS روی ماشین فیزیکی/مجازی جدا اجرا
می‌شود، پورت `11005` می‌تواند در هر دو یکسان بماند چون IP آن‌ها فرق دارد. کافی‌ست
هر Bridge به IP درست وصل شود.

**گزینهٔ ۲ (قوی‌تر — حتی روی یک ماشین):** در `atc_extras.py` پورت را از یک متغیر
محیطی بخوانید:
```python
import os
EXTRAS_PORT = int(os.environ.get("ATC_EXTRAS_PORT", "11005"))
...
self.socket.bind(f"tcp://*:{EXTRAS_PORT}")
```
اجرا برای گروه ۲:
```bash
ATC_EXTRAS_PORT=12005 python BlueSky.py
```

#### ه) دو نسخه از `bluesky-bridge.py`

`bridge-group1.py`:
```python
ZMQ_CORE_HOST   = "tcp://192.168.1.10:11000"
ZMQ_EXTRAS_HOST = "tcp://192.168.1.10:11005"
WS_PORT         = 8080
```

`bridge-group2.py`:
```python
ZMQ_CORE_HOST   = "tcp://192.168.1.20:12000"
ZMQ_EXTRAS_HOST = "tcp://192.168.1.20:12005"
WS_PORT         = 8081
```
(اگر هر دو bridge روی یک ماشین اجرا می‌شوند، `WS_PORT` حتماً باید متفاوت باشد.)

اجرا:
```bash
# ترمینال ۱
python bridge-group1.py
# ترمینال ۲
python bridge-group2.py
```

#### و) کلاینت‌های هر گروه

**RCP1, BD1, BD2** (گروه ۱) به `ws://192.168.1.10:8080` وصل می‌شوند.
**RCP2, BD3, BD4** (گروه ۲) به `ws://192.168.1.20:8081` وصل می‌شوند.

در `flat(BlueSky).html` هر گروه، مقدار `WS_URL` باید از قبل برای همان گروه تنظیم
شده باشد — پیشنهاد می‌شود این مقدار را به یک **پارامتر URL** تبدیل کنید تا یک فایل
HTML واحد برای هر دو گروه کار کند:
```
http://server/flat(BlueSky).html?ws=192.168.1.10:8080
http://server/flat(BlueSky).html?ws=192.168.1.20:8081
```
```js
const params = new URLSearchParams(window.location.search);
const WS_URL = "ws://" + (params.get("ws") || "localhost:8080");
```

### ۴. تنظیمات شبکه

| مورد | گروه ۱ | گروه ۲ |
|---|---|---|
| Firewall inbound (روی هر ماشین BS) | `11000,11001,11005/tcp` | `12000,12001,12005/tcp` |
| Firewall inbound (روی هر ماشین Bridge) | `8080/tcp` | `8081/tcp` |
| IP ثابت | 192.168.1.10 | 192.168.1.20 |
| ایزوله‌سازی (اختیاری، برای امنیت/وضوح بیشتر) | VLAN جدا یا subnet جدا برای هر گروه، با یک روتر بین‌شان در صورت نیاز به ترافیک مشترک (مثلاً یک RCP مرکزی ناظر بر هر دو) |

**نکتهٔ عملی مهم:** اگر بخواهید بعداً یک RCP «نظارتی» داشته باشید که هر دو گروه را
هم‌زمان ببیند، کافی‌ست آن مرورگر دو تب باز کند — یکی به `8080` یکی به `8081` — چون
هر گروه کاملاً مستقل و بدون تداخل داده کار می‌کند.

---

## چک‌لیست پیش از اجرا (هر دو حالت)

- [ ] IP ماشین‌ها ثابت شده (Static/Reservation) — تغییر IP یعنی خرابی اتصال
- [ ] `enable_discovery = False` در همهٔ `settings.cfg` ها (برای جلوگیری از ابهام بین دو BS در حالت ب)
- [ ] فایروال پورت‌های لازم را برای هر جهت باز کرده (نه فقط outbound)
- [ ] هر BlueSky از working directory مخصوص به خودش اجرا می‌شود
- [ ] در حالت ب، هیچ دو سرویسی (BS/Bridge) روی یک ماشین از یک پورت مشترک استفاده نمی‌کنند
- [ ] تست اتصال با `nc -zv <ip> <port>` قبل از تست کامل انجام‌شده
- [ ] `PLUGIN LOAD ATC_EXTRAS` بعد از هر بار اجرای BlueSky در کنسول تأیید شده (یا `enabled_plugins` در cfg درست است)

---

## پرسش‌های باز برای تصمیم بعدی

- آیا لایهٔ میانی فرمان BD (که هنوز طراحی نشده) هم باید per-group مجزا باشد؟ (به‌نظر
  می‌رسد بله — منطقاً باید کنار هر Bridge/BS خودش اجرا شود.)
- آیا RCP native GUI بومی BlueSky (Qt) هم استفاده می‌شود، یا فقط `flat(BlueSky).html`؟
  اگر GUI بومی هم لازم است، همان مکانیزم `--client <IP>` با پورت‌های `recv_port`/`send_port`
  همان گروه استفاده می‌شود.
